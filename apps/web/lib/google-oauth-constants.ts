// Shared between app/settings/actions.ts (a "use server" file, which may
// only export async functions) and the OAuth callback route handler.
export const GOOGLE_OAUTH_STATE_COOKIE = "google_oauth_state";
